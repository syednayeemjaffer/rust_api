[package]
name = "rust_api"
version = "0.1.0"
edition = "2024"

[dependencies]
# 🚀 Web framework
actix-web = "4"
actix-multipart = "0.6"

# ⚙️ Async utilities
futures-util = "0.3"

# 🗄️ Database (PostgreSQL + Diesel + connection pool)
diesel = { version = "2.2", features = ["postgres", "r2d2", "chrono"] }
dotenvy = "0.15"

# 🕒 Date/time handling
chrono = { version = "0.4", features = ["serde"] }

# 🧩 Validation and regex
regex = "1"

# 🔐 Password hashing
bcrypt = "0.15"

# 📦 Serialization / Deserialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# 🧠 Logging (optional but helpful)
env_logger = "0.11"
log = "0.4"

# 📂 File operations
tokio = { version = "1", features = ["fs", "macros", "rt-multi-thread"] }


actix-files = "0.6"

jsonwebtoken = "9"
watch = "0.2.3"
